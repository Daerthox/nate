import { tagElement, tagElementWithContent } from '../../src/tagging'

describe('tagging API', () => {
    describe('tagElement()', () => {
        it('should throw an error when no element matches the selector', () => {
            throw Error('not implemented')
        })
        it('should throw an error when more then one element matches the selector', () => {
            throw Error('not implemented')
        })
        it('should tag the element targeted by the selector', () => {
            throw Error('not implemented')
        })
    })
    describe('tagElementWithContent()', () => {
        it('should throw an error when no element with the specified content matches the selector', () => {
            throw Error('not implemented')
        })
        it('should throw an error when more then one element with the specified content matches the selector', () => {
            throw Error('not implemented')
        })
        it('should tag the element targeted by the selector if it has the specified content', () => {
            throw Error('not implemented')
        })
    })
})